package hashing;

import moves.Move;
import representation.Board;

public interface Hasher<B extends Board> {

	public long getHash(B board);
	
	public default long getNextHash(B previous, B next, Move transition) {
		return getHash(next);
	}
}
