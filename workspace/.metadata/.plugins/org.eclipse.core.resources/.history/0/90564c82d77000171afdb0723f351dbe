package dataStructures;

import java.util.function.Function;
import java.util.function.Predicate;

public class Ring<E> extends EnumSequence<E> {

	
	public Ring(Class<E> type, int maxSize, Function<E, E> getPreviousPossibleValue, Predicate<E> isPossibleValue) {
		super(type, maxSize, getPreviousPossibleValue, isPossibleValue);
	}

}
